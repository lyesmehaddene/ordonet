<header class="navbar">
    <div class="logo">
      <%= link_to root_path do %>
        <%= image_tag 'logo_blue.png', alt:'Ordonet Logo' %>
      <% end %>
    </div>
    <ul class="nav-links">
      <% if user_signed_in? %>
       <%= link_to "Dashboard", dashboard_path, class: 'primary-btn' %>
        <%= link_to "Profile", root_path, class: 'primary-btn' %>
        <%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete}, class: 'secondary-btn' %>
      <% else %>
        <%= link_to 'Connexion', new_user_session_path, class: 'primary-btn' %>
        <%= link_to 'S\'inscrire', new_user_registration_path, class: 'secondary-btn' %>
      <% end %>
    </ul>
  </header>
